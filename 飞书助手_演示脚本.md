# 飞书超级助手 — 演示视频脚本

## 录制前准备

| 项目 | 说明 |
|------|------|
| 飞书客户端 | 打开，进入有机器人的测试群 |
| 飞书日历 | 打开备用（演示 /schedule 时切屏） |
| Bot 项目 | IDE 中启动，确认 8080 端口正常 |
| natapp | 启动隧道，确认飞书回调 URL 可达 |
| MySQL / Redis | Docker 启动（docker compose up -d） |
| 翻译素材 | 提前准备一句英文：`Artificial intelligence is transforming enterprise productivity tools` |
| Gitee | 浏览器打开仓库页面备用（演示 gitlog/gitdiff 时切屏） |

---

## 第一部分：基础指令（0:00 - 1:30）

### 1. 开场（0:00-0:15）

> 大家好，今天演示的是「飞书超级助手」——基于 Spring Boot 3 的企业效率机器人。所有功能在飞书群聊里通过斜杠指令触发。

### 2. /help — 查看所有指令（0:15-0:30）

```
/help
```

> 机器人列出 11 条可用指令及用法。

### 3. /weather — 实时天气（0:30-1:00）

> 查询实时天气。注意：本系统只支持实时天气，不支持未来天气预报。

```
/weather 北京
```

```
/weather 深圳
```

### 4. 多轮对话（1:00-1:30）

> 首次输入指令后，后续不用再打斜杠，AI 理解上下文自动路由。背后是 Redis 存储对话上下文 + AI 意图解析。

```
/weather 上海
```

> 等机器人返回后，直接发：

```
深圳呢
```

> 不再需要 `/weather` 前缀，AI 自动识别意图。

---

## 第二部分：企业效率（1:30 - 4:00）

### 5. /translate — 翻译（1:30-2:00）

> 多语种互译。提前备好英文句子，复制粘贴：

```
/translate Artificial intelligence is transforming enterprise productivity tools to 中文
```

> 中日互译：

```
/translate 你好世界 to 日文
```

### 6. /schedule — 创建日程（2:00-2:30）

> 创建日程并同步到飞书日历。

```
/schedule 明天下午3点 项目评审会议
```

> （切屏到飞书日历展示创建的日程）

### 7. /group — 创建群聊并拉人（2:30-3:00）

> 创建群聊。支持按姓名或 open_id 拉指定成员，发指令的人自动进群。

```
/group 项目讨论组 张三 李四
```

> 机器人调飞书通讯录按姓名查到 open_id，把张三、李四一起拉进新建的群。

### 8. /broadcast — 消息广播（3:00-3:30）

> 向指定群发送广播通知，需 ADMIN 以上权限。

```
/broadcast 技术交流群 | 重要通知 | 今天下午5点进行服务升级，请大家提前保存工作
```

> 机器人通过群名匹配 chat_id，推送消息到目标群。

### 9. /search 和 /search-ai — 知识库（3:30-4:00）

> 知识库搜索。

```
/search 数据库设计
```

> 返回匹配的文档列表。

> AI 理解知识库内容后回答：

```
/search-ai MyBatis-Plus 怎么配置逻辑删除
```

> AI 阅读理解知识库内容后给出精准回答，不是简单关键词匹配。

---

## 第三部分：高级功能（4:00 - 6:00）

### 10. /gitlog 和 /gitdiff — Git 操作（4:00-4:30）

> 查看仓库最近提交。

```
/gitlog feishu_super_bot
```

> 返回提交记录：作者、时间、提交信息。

> 查看某次提交的代码变更：

```
/gitdiff feishu_super_bot 05fcc6e
```

> 返回完整 diff。

### 11. /review — AI 代码审查（4:30-5:00）

> 贴一段代码，AI 给出审查意见。准备一段代码：

```
/review 
@Service
public class UserService {
    public User getUser(Long id) {
        return userMapper.selectById(id);
    }
    public void updateUser(User user) {
        userMapper.updateById(user);
    }
}
```

> AI 分析代码质量、命名规范、安全性，给出改进建议。

### 12. 审批催办（5:00-6:00）

> **重要说明**：飞书的「审批实例状态变更」事件在提交请假时不会触发，`leave_approval` 事件仅在审批通过后才推送。因此演示中手动写入一条待审批记录模拟"有人提交了请假但领导还没批"，定时任务扫描到后自动催办。

> ——切屏 MySQL——（执行插入）

```sql
INSERT INTO bot_approval_reminder (approval_id, approver_open_id, applicant_name, title, status, remind_count)
VALUES ('DEMO-001', 'ou_b6c1f63694dd25caff3ad4463ce87452', '张三', '事假', 1, 0);
```

> ——切回飞书——10 秒内机器人发送私聊催办：

> ⚠ 审批提醒 —— 你有一条待审批：**事假**，申请人：张三，这是第 1 次提醒，请及时处理

> 每 10 秒提醒一次，最多 5 次。审批人处理后停止催办。

### 13. 自动代码审查（可选 6:00-6:30）

> Gitee push 后 Webhook 自动触发 AI 审查，结果推送到飞书群。

> ——在 Gitee 仓库 push 一次提交——（提前准备）

> 飞书群自动收到审查结果：变更摘要、代码质量分析、改进建议。

---

## 结尾（6:30-6:50）

> 以上就是飞书超级助手的功能演示。
> 
> 技术栈：Spring Boot 3.3 + Maven 多模块、MySQL + MyBatis-Plus、Redis + Redisson、DeepSeek AI、飞书 SDK + Webhook。
> 
> 项目地址见简介，欢迎 Star。感谢观看。

---

## 录制速查表

按顺序输入，一次录完不剪辑：

| 序号 | 输入 | 位置 |
|------|------|------|
| 1 | `/help` | 基础 |
| 2 | `/weather 北京` | 基础 |
| 3 | `/weather 深圳` | 基础 |
| 4 | `/weather 上海` | 基础 |
| 5 | `深圳呢` | 多轮对话 |
| 6 | `/translate Artificial intelligence is transforming enterprise productivity tools to 中文` | 企业效率 |
| 7 | `/translate 你好世界 to 日文` | 企业效率 |
| 8 | `/schedule 明天下午3点 项目评审会议` | 企业效率 |
| 9 | `/group 项目讨论组 张三` | 企业效率 |
| 10 | `/broadcast 技术交流群 \| 重要通知 \| 今天下午5点进行服务升级，请大家提前保存工作` | 企业效率 |
| 11 | `/search 数据库设计` | 企业效率 |
| 12 | `/search-ai MyBatis-Plus 怎么配置逻辑删除` | 企业效率 |
| 13 | `/gitlog feishu_super_bot` | 高级 |
| 14 | `/gitdiff feishu_super_bot 05fcc6e` | 高级 |
| 15 | `/review <粘贴代码>` | 高级 |
| 16 | 手动插库 DEMO-001 | 高级 |

---

## 注意事项

- **天气**：只支持实时天气，脚本中无"明天呢"追问
- **翻译**：英文句子提前备好，复制粘贴
- **审批**：脚本中明确说明飞书限制，手动插库演示催办效果
- **多轮对话**：先 `/weather 上海`，再发 `深圳呢`，AI 自动理解
- **/group**：需要通讯录权限 `contact:contact.base:readonly` 才能按姓名查找
