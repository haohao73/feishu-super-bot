# 飞书超级助手 — 答辩脚本

## 答辩准备

- IDEA 打开项目（关闭不需要的文件，只留关键类）
- 飞书客户端打开测试群
- 浏览器打开 Gitee 仓库备用
- 环境确认：bot 已启动、natapp 在线、MySQL/Redis 运行中

---

## 一、项目介绍（2分钟）

> 【投屏：IDEA 项目目录树】

大家好，我的项目是「飞书超级助手」——一个基于 Spring Boot 3 的企业效率飞书群机器人。

用户在飞书群里用斜杠指令就能触发各种功能：查天气、翻译、建日程、AI 代码审查、审批催办等等。一共 12 个指令，4 个系统功能。

技术栈是 Spring Boot 3.3 + Maven 7 模块 + MySQL + Redis + DeepSeek AI + WebClient。这是项目结构——api 做入口、core 写业务、plugin 定义接口、integration 调第三方 API、auth 鉴权、infrastructure 数据层。

核心设计是**插件化指令框架**——想要加一个新指令，只需要建一个类实现 `CommandPlugin` 接口，标 `@Component`。不改任何已有代码。CommandRouter 自动发现、自动收录、`/help` 自动展示。

---

## 二、一条指令怎么走通（3分钟）

> 【投屏：WebhookController.java】

我以 `/weather 北京` 为例，从头到尾走一遍一条指令的完整链路。

第一步，用户在飞书群里发 `/weather 北京`。飞书服务器收到后，通过 Webhook 推送到我们的服务器。因为我们是本地开发，用 natapp 做了内网穿透——`feishubot.nat300.top` 映射到 `localhost:8080`。

第二步，`WebhookController` 的 `onEvent()` 方法接收这个 POST 请求。做三件事：读取请求体、HMAC 签名验证——确保请求真是飞书发的不是伪造的、然后立刻返回 200——因为飞书要求 3 秒内响应，不然会超时重试。真正的业务处理丢到线程池里异步执行。

第三步，`processCommand()` 里先自动注册用户——第一次发消息的用户会自动写入 `bot_user` 表，分配默认的 USER 角色。然后 `CommandContext.parse()` 解析消息文本——把 `/weather 北京` 拆成 command="weather" 和 args="北京"。

第四步，`CommandRouter.route()` 收到 command="weather"，从它维护的 `Map<String, CommandPlugin>` 里 O(1) 找到 `WeatherHandler`，调它的 `execute()` 方法。

第五步，`WeatherClient` 用 WebClient 调 Open-Meteo 天气 API，拿到 JSON 响应。把温度、湿度、天气代码映射成中文描述，拼成 Markdown 回复。

第六步，`FeishuClient.sendTextMessage()` 把回复通过飞书 API 发回群里。同时把这次对话存入 Redis——10 分钟有效，下次用户直接说"那明天呢"，AI 能从上下文里理解他在延续天气话题。

**这条链路涵盖了 Webhook 接收、异步处理、插件路由、第三方 API 调用、鉴权、多轮对话——是项目的核心技术骨架。**

---

## 三、Webhook 怎么实现的（2分钟）

> 【投屏：WebhookController + SignatureVerifier + GiteeWebhookController】

项目里有两个 Webhook 端点。

第一个是飞书的 `/webhook/event`。接收用户消息、审批事件。核心逻辑刚才已经讲了。

重点讲一下鉴权——飞书用的是 HMAC-SHA256 签名验证。原理很简单：飞书和我们各有一个暗号 `encrypt_key`，飞书发消息时用「时间戳+随机数+密钥+消息体」算一个签名放在 HTTP Header 里，我们收到后用同样的算法也算一遍，两个签名一致才放行。这防了两件事：消息篡改和重放攻击——时间戳超过一小时直接拒绝。

第二个是 Gitee 的 `/webhook/gitee`。配置在 Gitee 仓库的 Webhooks 里。每次 push 代码，Gitee 推送一个 JSON，包含 before、after（push 前后的 commit SHA）和 commits 数组。

但这个 JSON 里只有"谁提交了什么"，没有"具体改了什么代码"。所以我们再调一次 Gitee Compare API——用 before 和 after 的 SHA 去拿完整的代码 diff，拼成文本后发给 DeepSeek AI 审查，审查结果发到飞书群。

**两个 Webhook，一样的模式：读body → 验签 → 返回200 → 异步处理。**

---

## 四、项目亮点（2分钟）

> 【投屏：不用专门开文件，口头说】

第一个亮点是**插件化架构**。这个架构的核心就是 `CommandPlugin` 接口。加新指令只需建一个类实现它。`CommandRouter` 在启动时用 `List<CommandPlugin>` 自动收集所有实现类，构建成 Map。这不是 if-else 路由——是真正基于接口的动态发现。也是开闭原则的落地：对扩展开放，对修改关闭。

第二个亮点是**全项目 WebClient 迁移**。最初所有 HTTP 调用用的是 RestTemplate。后来把 6 个 Client 类全部迁移到 WebClient，净删了将近 200 行代码。WebClient 用 `.bodyToMono()` 一行完成 JSON 自动反序列化，不需要手动逐字段 set。

第三个亮点是**审批催办的三层兜底策略**。因为飞书的审批事件 open_id 存在跨应用问题——审批事件里的 open_id 不能直接用来发私聊。所以我们做了三层：第一层，查 bot_user 表找注册用户的 open_id 发私聊；第二层，找不到就用任意已注册用户的 open_id 发；第三层，都失败就发到审查群。确保消息不丢。

第四个是**Gitee Webhook + AI 代码审查**的联动。push → Webhook → Compare API → diff → AI → 飞书群。两步 API 调用 + 一次 AI 调用，全自动。

---

## 五、投屏演示流程

### 5.1 IDEA 项目结构（开场）

1. 展开项目目录树
2. 点开 `CommandPlugin.java`（接口定义）
3. 点开 `CommandRouter.java`（路由收集逻辑）
4. 点开 `WeatherHandler.java`（一个指令的完整实现）

**口播**：看这个接口——三个方法，name、description、execute。WeatherHandler 实现它。CommandRouter 启动时自动扫描所有实现类放进 Map。加指令不改已有代码。

### 5.2 飞书群实测（核心演示）

1. 切到飞书测试群
2. 发 `/weather 北京` → 展示返回结果
3. 发 `深圳呢` → 展示多轮对话（不用再打 /weather）
4. 发 `/translate Hello to 中文` → 展示翻译
5. （可选）发 `/gitlog haohao73/feishu_bot` → 展示 Git 集成

### 5.3 关键代码（被问细节时打开）

- `WebhookController.onEvent()`：指着"3秒返回 + 异步处理"说
- `SignatureVerifier.verify()`：指着 HMAC 算法说
- `CommandRouter` 构造函数：指着 `Collectors.toMap` 说自动发现

### 5.4 架构图（如果准备了）

画板上或 PPT 上放这张图：

```
飞书用户 → Webhook → natapp → WebhookController
                                    │
                              processCommand()
                                    │
                    ┌───────────────┼───────────────┐
                    │               │               │
              CommandRouter    DialogService    GroupRegistry
                    │         (多轮对话)       (群名采集)
              ┌─────┴─────┐
          WeatherHandler  ...
              │
         WeatherClient → Open-Meteo API
              │
         FeishuClient → 发回复到飞书群
```

---

## 六、时间分配

| 部分 | 时间 | 备注 |
|------|------|------|
| 项目介绍 + 架构 | 2分钟 | 打开 IDEA |
| 一条指令走通 | 3分钟 | 核心，讲细一点 |
| Webhook 实现 | 2分钟 | 飞书 + Gitee 两个 |
| 项目亮点 | 2分钟 | 四个亮点挑 2-3 个说 |
| 演示 | 3分钟 | 飞书群实测 |
| 总计 | 12分钟 | 控制在 15 分钟内 |

